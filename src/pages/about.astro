---
import BaseLayout from "../layouts/BaseLayout.astro";
import { Image } from "astro:assets";
import cat from "../assets/404cat/cat2.jpg";

const pageTitle = "About Me";

const identity = {
  firstName: "Ваня",
  country: "РФ",
  occupation: "начинающим программистом",
  hobbies: ["заниматься музыкой", "новые технологии"],
};

const skills = ["HTML", "CSS", "JavaScript", "Astro", "Python/Flask"];

const happy = true;
const finished = true;
const goal = 3;

const skillColor = "var(--five-color)";
const fontWeight = "bold";
const textCase = "uppercase";
---

<BaseLayout pageTitle={pageTitle}>
  <h2>Про меня</h2>
  <div class="container">
    <Image
      src={cat}
      alt="Кот, но потом буду я"
      width={300}
      height={300}
      class="image"
    />
  </div>
  <p>
    I am working through Astro's introductory tutorial. This is the second page
    on my website, and it's the first one I built myself!
  </p>
  <p>
    This site will update as I complete more of the tutorial, so keep checking
    back and see how my journey is going!
  </p>
  <p>Несколько фактов про меня:</p>
  <ul>
    <li>Меня зовут {identity.firstName}.</li>
    <li>
      Я живу в {identity.country} и хотел бы работать программистом, пока я только
      начинаю.
    </li>
    {
      identity.hobbies.length >= 2 && (
        <li>
          Мои хобби сейчас: {identity.hobbies[0]} и {identity.hobbies[1]}.
        </li>
      )
    }
  </ul>
  <p>Мои навыки:</p>
  <ul>
    {skills.map((skill) => <li class="skill">{skill}</li>)}
  </ul>
</BaseLayout>

<style define:vars={{ skillColor, fontWeight, textCase }}>
  .skill {
    color: var(--skillColor);
    font-weight: var(--fontWeight);
    text-transform: var(--textCase);
  }

  * {
    font-family: var(--font-read);
  }

  .container {
    margin: 0 auto;
    margin-bottom: 5%;
  }

  .image {
    object-fit: cover;
    border-radius: 50%;
    height: 200px;
    width: 200px;
  }
</style>
